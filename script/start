#!/usr/bin/env bash
set -ex

./script/prepare-env

./node_modules/.bin/concurrently \
  './node_modules/.bin/nodemon --exec ./node_modules/.bin/graphql-codegen "./src/**/*.graphql"' \
  './node_modules/.bin/react-scripts-vite start' \
  './node_modules/.bin/tauri dev'
